<Window x:Class="lab2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab2"
        mc:Ignorable="d"
        Title="Rose Curve Lab" Height="900" Width="1300" 
        MinHeight="860" MinWidth="1300"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#DDDDDD">

    <Window.Resources>
        <!-- КОЛЬОРИ LOFT -->
        <SolidColorBrush x:Key="DarkBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PanelBg" Color="#252526"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF8C00"/> <!-- Industrial Orange -->
        <SolidColorBrush x:Key="TextLight" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="ControlBg" Color="#333337"/>
        <SolidColorBrush x:Key="BorderColor" Color="#555555"/>

        <!-- СТИЛЬ ДЛЯ LABEL -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- СТИЛЬ ДЛЯ TEXTBOX -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ДЛЯ BUTTON -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="#1E1E1E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CC7000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ДЛЯ CHECKBOX -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- СТИЛЬ ДЛЯ ЗАГОЛОВКІВ -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10,20,0,10"/>
            <Setter Property="TextDecorations" Value="Underline"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/> <!-- Більше місця для малювання -->
            <ColumnDefinition Width="1*"/>   <!-- Панель керування -->
        </Grid.ColumnDefinitions>

        <!-- ЛІВА ЧАСТИНА: ПОЛОТНО -->
        <Border Grid.Column="0" Margin="20" Background="#2D2D30" CornerRadius="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.5" ShadowDepth="5"/>
            </Border.Effect>
            <!-- Canvas білий, як папір на столі -->
            <Canvas x:Name="Cnv" Background="#F5F5F5" ClipToBounds="True"/>
        </Border>

        <!-- ПРАВА ЧАСТИНА: ПАНЕЛЬ КЕРУВАННЯ -->
        <Border Grid.Column="1" Background="{StaticResource PanelBg}" BorderBrush="#3E3E42" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15">
                    
                    <!-- 1. ROSE PARAMETERS -->
                    <TextBlock Text="ROSE PARAMETERS" Style="{StaticResource HeaderStyle}" Margin="10,0,0,10"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <!-- Radius A -->
                        <Label Grid.Row="0" Grid.Column="0">Radius (A):</Label>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ABox" TextChanged="ABox_OnTextChanged" Text="100"/>

                        <!-- Coefficient K -->
                        <Label Grid.Row="0" Grid.Column="2">Coeff (K):</Label>
                        <TextBox Grid.Row="0" Grid.Column="3" x:Name="KBox" TextChanged="KBox_OnTextChanged" Text="3"/> <!-- НОВЕ ПОЛЕ ДЛЯ K -->

                        <!-- Calculated Info -->
                        <Label Grid.Row="1" Grid.Column="0">Area (S):</Label>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="SBox" IsReadOnly="True" Foreground="#AAAAAA"/>
                        
                        <Label Grid.Row="1" Grid.Column="2">Length (L):</Label>
                        <TextBox Grid.Row="1" Grid.Column="3" x:Name="LBox" IsReadOnly="True" Foreground="#AAAAAA"/>
                    </Grid>

                    <Separator Background="#3E3E42" Margin="0,20"/>

                    <!-- 2. ANIMATION -->
                    <TextBlock Text="ANIMATION" Style="{StaticResource HeaderStyle}" Margin="10,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" Content="ANIMATE K" Click="ButtonAnimation_OnClick" Margin="2"/>
                        <Button Grid.Column="1" Content="DRAW TOP" Click="ButtonTop_OnClick" Margin="2"/> <!-- Для тестування точки -->
                        <Button Grid.Column="2" Content="PAUSE" Click="ButtonPauseAnimation_OnClick" Margin="2"/>
                    </Grid>

                    <Separator Background="#3E3E42" Margin="0,20"/>

                    <!-- 3. TRANSFORMATIONS (EUCLIDEAN) -->
                    <TextBlock Text="TRANSFORMATIONS" Style="{StaticResource HeaderStyle}" Margin="10,0,0,10"/>

                    <!-- Move -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="X:"/>
                        <TextBox Grid.Column="1" x:Name="MXox" Text="10" Margin="2"/>
                        
                        <Label Grid.Column="2" Content="Y:"/>
                        <TextBox Grid.Column="3" x:Name="MYox" Text="10" Margin="2"/>
                        
                        <Button Grid.Column="4" Content="MOVE" Width="60" Click="ButtonMove_OnClick" Margin="5,0,0,0"/>
                    </Grid>

                    <!-- Rotate -->
                    <Label Content="Rotation (Center X, Y &amp; Angle)" HorizontalAlignment="Left" Margin="5,5,0,5" Foreground="#AAAAAA" FontSize="10"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="XRBox" Text="50" Margin="2"/>
                        <TextBox Grid.Column="1" x:Name="YRBox" Text="25" Margin="2"/>
                        <TextBox Grid.Column="2" x:Name="AngleBox" Text="30" Margin="2"/>
                        <Button Grid.Column="3" Content="ROTATE" Width="60" x:Name="rot_bt" Click="Rot_bt_OnClick" Margin="5,0,0,0"/>
                    </Grid>

                    <Separator Background="#3E3E42" Margin="0,20"/>

                    <!-- 4. EXTRAS -->
                    <TextBlock Text="VISUALIZATION" Style="{StaticResource HeaderStyle}" Margin="10,0,0,10"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="AsymptCheckBox" Content="Show Tangent/Normal" Checked="AsymptCheckBox_OnChecked" Unchecked="AsymptCheckBox_OnChecked"/>
                        <!-- Додаткові чекбокси можна додати тут -->
                    </StackPanel>

                    <Button Content="RESET VIEW" Click="ButtonPause_OnClick" Margin="0,20,0,0" Height="35"/>
                    
                    <Border Height="50"/> 
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>