<Window x:Class="lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab3"
        mc:Ignorable="d"
        Title="Lab 3: Curves Modeling" Height="900" Width="1300" 
        MinHeight="860" MinWidth="1300"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#DDDDDD">

    <Window.Resources>
        <!-- КОЛЬОРИ -->
        <SolidColorBrush x:Key="DarkBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PanelBg" Color="#252526"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF8C00"/> <!-- Industrial Orange -->
        <SolidColorBrush x:Key="TextLight" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="ControlBg" Color="#333337"/>
        <SolidColorBrush x:Key="BorderColor" Color="#555555"/>

        <!-- СТИЛЬ ДЛЯ LABEL -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- СТИЛЬ ДЛЯ TEXTBOX -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBg}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ДЛЯ BUTTON -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="#1E1E1E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CC7000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- СТИЛЬ ДЛЯ CHECKBOX -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- СТИЛЬ ДЛЯ ЗАГОЛОВКІВ -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10,20,0,10"/>
            <Setter Property="TextDecorations" Value="Underline"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/> <!-- Canvas -->
            <ColumnDefinition Width="1*"/>   <!-- Controls -->
        </Grid.ColumnDefinitions>

        <!-- Canvas Section -->
        <Border Grid.Column="0" Margin="20" Background="#2D2D30" CornerRadius="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.5" ShadowDepth="5"/>
            </Border.Effect>
            <!-- Біле полотно для малювання -->
            <Canvas x:Name="Cnv" Background="#F5F5F5" ClipToBounds="True"/>
        </Border>

        <!-- Controls Section -->
        <Border Grid.Column="1" Background="{StaticResource PanelBg}" BorderBrush="#3E3E42" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15">
                    
                    <!-- 1. CURVE SETTINGS -->
                    <TextBlock Text="CURVE SETTINGS (Hyperbola)" Style="{StaticResource HeaderStyle}"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Вага (Weight) для раціональної кривої Безьє -->
                        <Label Grid.Column="0" Content="Weight (w > 1):"/>
                        <TextBox Grid.Column="1" x:Name="WeightBox" Text="2.0" TextChanged="WeightBox_OnTextChanged" Margin="0,0,0,5"/>
                    </Grid>
                    <TextBlock Text="* w > 1 for Hyperbola arc" FontSize="10" Foreground="Gray" Margin="0,0,0,10" HorizontalAlignment="Right"/>

                    <!-- VISUALIZATION -->
                    <TextBlock Text="VISUALIZATION" Style="{StaticResource HeaderStyle}"/>
                    
                    <StackPanel>
                        <!-- Чекбокс для показу каркасу -->
                        <CheckBox x:Name="ShowSkeletonCheck" Content="Show Control Polygon" 
                                  IsChecked="True" Checked="ShowSkeletonCheck_OnChecked" Unchecked="ShowSkeletonCheck_OnChecked"/>
                        
                        <!-- Чекбокс для режиму редагування точок -->
                        <CheckBox x:Name="EditModeCheck" Content="Enable Point Editing (Drag)" 
                                  IsChecked="True" Checked="EditModeCheck_OnChecked" Unchecked="EditModeCheck_OnChecked"/>
                    </StackPanel>

                    <Separator Background="#3E3E42" Margin="0,20"/>

                    <!-- 2. ANIMATION -->
                    <TextBlock Text="ANIMATION" Style="{StaticResource HeaderStyle}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" Content="MORPH SHAPE" Click="ButtonAnimate_OnClick" Margin="2"/>
                        <Button Grid.Column="1" Content="RESET" Click="ButtonReset_OnClick" Margin="2"/>
                    </Grid>

                    <Separator Background="#3E3E42" Margin="0,20"/>

                    <!-- 3. TRANSFORMATIONS -->
                    <TextBlock Text="TRANSFORMATIONS" Style="{StaticResource HeaderStyle}"/>

                    <!-- Move -->
                    <Label Content="Move (X, Y)" HorizontalAlignment="Left" Foreground="#AAAAAA" FontSize="10"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="MXox" Text="10" Margin="2"/>
                        <TextBox Grid.Column="1" x:Name="MYox" Text="10" Margin="2"/>
                        <Button Grid.Column="2" Content="MOVE" Width="60" Click="ButtonMove_OnClick" Margin="5,0,0,0"/>
                    </Grid>

                    <!-- Rotate -->
                    <Label Content="Rotate (Center X, Y &amp; Angle)" HorizontalAlignment="Left" Foreground="#AAAAAA" FontSize="10"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="XRBox" Text="400" Margin="2"/> <!-- Центр повороту -->
                        <TextBox Grid.Column="1" x:Name="YRBox" Text="400" Margin="2"/>
                        <TextBox Grid.Column="2" x:Name="AngleBox" Text="30" Margin="2"/>
                        <Button Grid.Column="3" Content="ROTATE" Width="60" x:Name="rot_bt" Click="Rot_bt_OnClick" Margin="5,0,0,0"/>
                    </Grid>
                    
                    <!-- Scale & Mirror -->
                    <Label Content="Tools" HorizontalAlignment="Left" Foreground="#AAAAAA" FontSize="10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Row="0" Grid.Column="0" Content="Sym Center:"/>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="SymXBox" Text="400" Margin="2"/>
                        <TextBox Grid.Row="0" Grid.Column="2" x:Name="SymYBox" Text="400" Margin="2"/>
                        <Button Grid.Row="0" Grid.Column="3" Content="MIRROR" Click="Symm_bt_OnClick" Width="70" Margin="5,2"/>

                        <Label Grid.Row="1" Grid.Column="0" Content="Scale Coeff:"/>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ScaleBox" Text="1.2" Margin="2"/> <!-- Одне число для масштабу -->
                        <Button Grid.Row="1" Grid.Column="3" Content="SCALE" Click="Scale_bt_OnClick" Width="70" Margin="5,2"/>
                    </Grid>

                    <Border Height="50"/> 
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>